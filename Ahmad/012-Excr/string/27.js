// 27. Write a js program to replace all occurrences of a character with another in a string.

const search = " ";
const replaceWith = "-";
const result = "duck duck go";

console.log(result.split(search).join(replaceWith));
