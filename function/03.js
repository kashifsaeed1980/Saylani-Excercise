// 3. Write a js program to find maximum and minimum between two numbers using functions.

function comp(a, b) {
  console.log("Max Number = " + Math.max(a, b));
  console.log("Min Number = " + Math.min(a, b));
}

comp(135, 50);
